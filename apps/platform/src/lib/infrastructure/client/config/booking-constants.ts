/**
 * Booking configuration constants
 * These values control warnings and recommendations for the booking flow
 */

/**
 * Minimum number of hours in advance that is recommended for booking a coaching session.
 * Booking with less notice will show a warning (but still allow the booking).
 * Set to 0 to disable the warning.
 */
export const MIN_ADVISED_BOOKING_HOURS = 3;

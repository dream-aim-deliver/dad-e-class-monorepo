'use client';

// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/Order-history-Orders-Payments-Student-workspace-6bb2794c7331418e8fbbf83c40dcb1e5
// Usecases: listUserIncomingTransactions, generateInvoicePdf
// Features: View transaction history, Download/Generate invoices
// API Endpoints: GET /{api_version}/{platform_id}/user/{user_id}/billing
// User Types: Student
// Figma: https://www.figma.com/design/8KEwRuOoD5IgxTtFAtLlyS/Just_Do_Ad-1.2?node-id=7709-215954&t=U8ErZnsTA6OWWWKV-4

import { trpc } from '../trpc/cms-client';
import { useLocale, useTranslations } from 'next-intl';
import { TLocale } from '@maany_shr/e-class-translations';
import { DefaultLoading, DefaultError } from '@maany_shr/e-class-ui-kit';

interface OrderHistoryProps {
  locale: TLocale;
}

export default function OrderHistory({ locale }: OrderHistoryProps) {
  const t = useTranslations('pages.orderHistory');
  const currentLocale = useLocale() as TLocale;

  // TRPC query for listUserIncomingTransactions usecase
  const [transactionsResponse] = trpc.listUserIncomingTransactions.useSuspenseQuery({});


  return (
    <div className="flex flex-col space-y-5 px-30">
      {/* Page header with translations */}
      <div>
        <h1>{t('title')}</h1>
        <p>{t('description')}</p>
      </div>

      {/* TODO: Implement tab navigation for Order History and Payments for coaches only, students only see Order history */}

      {/* TODO: Implement client-side generateInvoicePdf */}
      {/* TODO: Add generate invoice buttons */}
    </div>
  );
}

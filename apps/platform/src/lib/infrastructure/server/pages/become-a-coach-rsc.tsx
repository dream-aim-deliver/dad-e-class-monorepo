// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/Become-a-coach-06c36feec8e340c79ca65f79971f1a20
// Usecases: listTopics, sendBecomeACoachEmail
// Features: listTopics, sendBecomeACoachEmail (client-side only - NOT YET IMPLEMENTED)
// UI Components: UserProfile (Personal information Form), UserProfile (Professional Information Form), Become a Coach Form, Professional Information Sent Modal
// User Types: Visitor

import { HydrateClient, prefetch, trpc } from '../config/trpc/cms-server';
import { Suspense } from 'react';
import DefaultLoadingWrapper from '../../client/wrappers/default-loading';
import BecomeACoach from '../../client/pages/become-a-coach';
import { TLocale } from '@maany_shr/e-class-translations';

interface BecomeACoachServerComponentProps {
  locale: TLocale;
}

export default async function BecomeACoachServerComponent(
  props: BecomeACoachServerComponentProps
) {
  // Streaming pattern: Fire prefetch without awaiting (TSK-PERF-007)
  prefetch(trpc.listTopics.queryOptions({})); // listTopics

  return (
    <HydrateClient>
      <Suspense fallback={<DefaultLoadingWrapper />}>
        <BecomeACoach locale={props.locale} />
      </Suspense>
    </HydrateClient>
  );
}

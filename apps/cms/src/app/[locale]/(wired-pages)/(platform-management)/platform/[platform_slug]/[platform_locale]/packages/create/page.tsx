// Auto-generated by page-scaffold command
// Notion: https://www.notion.so/CMS-Create-package-Manage-Packages-f3ea8a52ac704207bdfdec2bacfb37a1
// Route: /[platform_slug]/packages/create

// Update the import path below if the file exists at a different location
// Update the import path below to the correct location of create-package-rsc
import { TLocale } from '@maany_shr/e-class-translations';
import CreatePackageServerComponent from '../../../../../../../../../lib/infrastructure/server/pages/create-package-rsc';
import DefaultLoadingWrapper from '../../../../../../../../../lib/infrastructure/client/wrappers/default-loading';

import { Suspense } from 'react';

export default async function CreatePackagePage({
    params: paramsPromise,
    searchParams: searchParamsPromise,
}: {
    params: Promise<{
        locale: string;
        platform_slug: string;
        platform_locale: string;
    }>;
    searchParams: Promise<{ [key: string]: string | string[] | undefined }>;
}) {
    const params = await paramsPromise;
	await searchParamsPromise;

    return (
        <Suspense fallback={<DefaultLoadingWrapper />}>
            <CreatePackageServerComponent
                locale={params.locale as TLocale}
                platformSlug={params.platform_slug}
                platformLocale={params.platform_locale as TLocale}
            />
        </Suspense >
    );
}

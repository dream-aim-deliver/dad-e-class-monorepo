// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/CMS-Manage-Offers-Page-22d5a7432d01809487b5f5592b2013f3
// Usecases: saveOffersPage, listPackages, requestFileUpload, getOffersPageOutline, listOffersPagePackagesShort

import { Suspense } from 'react';
import ManageOffersPageServerComponent from '../../../../../../../../lib/infrastructure/server/pages/manage-offers-page-rsc';
import DefaultLoadingWrapper from '../../../../../../../../lib/infrastructure/client/wrappers/default-loading';

export default async function ManageOffersPage({
    params: paramsPromise,
}: {
    params: Promise<{
        locale: string;
        platform_slug: string;
        platform_locale: string;
    }>;
}) {
    const params = await paramsPromise;

    return (
        <Suspense fallback={<DefaultLoadingWrapper />}>
            <ManageOffersPageServerComponent
                locale={params.locale}
                platformSlug={params.platform_slug}
                platformLocale={params.platform_locale}
            />
        </Suspense>
    );
}

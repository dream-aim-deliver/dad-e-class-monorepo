// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/CMS-Manage-Coaching-Page-25d5a7432d01802cabf7fff03e8d1b9e

import ManageCoachingPageServerComponent from '../../../../../../../../lib/infrastructure/server/pages/manage-coaching-page-rsc';

interface ManageCoachingPageProps {
  params: Promise<{
    locale: string;
    platform_slug: string;
    platform_locale: string;
  }>;
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>;
}

export default async function ManageCoachingPage({
  params: paramsPromise,
  searchParams: searchParamsPromise,
}: ManageCoachingPageProps) {
  const params = await paramsPromise;
  const searchParams = await searchParamsPromise;

  const { locale, platform_slug, platform_locale } = params;

  return (
    <ManageCoachingPageServerComponent
      locale={locale}
      platformSlug={platform_slug}
      platformLocale={platform_locale}
    />
  );
}

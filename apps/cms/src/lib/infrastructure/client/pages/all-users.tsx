'use client';

// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/CMS-All-Users-Manage-Users-across-all-platforms-2245a7432d0180658aa3e5d86021683b
// Usecases from Notion: listUsers
// Features from Notion: FEAT-195 (listUsers - In Progress)
// User Types from Notion: CMS (admin/superadmin - middleware enforced)
// Figma: https://www.figma.com/design/8KEwRuOoD5IgxTtFAtLlyS/Just_Do_Ad-1.2?node-id=6498-107088&t=OB4RQdQpkBV9iLV7-4
// Tags: AG Grid, users
// Important Comments from Notion:
// - Can't access user details from this page; in the Grid don't show "Details"
// - NO send notifications functionality (users span multiple platforms, notifications need single platform context)
// - To send bulk notifications, admins must go to platform-specific user view

import { useLocale, useTranslations } from 'next-intl';
import { TLocale } from '@maany_shr/e-class-translations';

interface AllUsersProps {
  locale: TLocale;
}

export default function AllUsers({ locale }: AllUsersProps) {
  const t = useTranslations('pages.cmsAllUsers');
  const currentLocale = useLocale() as TLocale;

  return (
    <div className="flex flex-col space-y-5 px-30">
      <div>
        <h1>{t('title')}</h1>
        <p>{t('description')}</p>
      </div>

      {/* Features from Notion:
          - FEAT-195 (listUsers - In Progress)
      */}
      {/* UI Components from Notion:
          - AG Grid for user management (no details column, no send notifications)
          - 1 component referenced
      */}
    </div>
  );
}

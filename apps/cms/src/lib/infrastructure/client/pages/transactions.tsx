'use client';

// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/CMS-Transactions-22d5a7432d018022a18cc0587107aece
// Usecases from Notion: generateInvoicePdf, createOutgoingTransaction, listTransactions, deleteOutgoingTransaction, listPlatformCoaches
// Features from Notion: FEAT-198 (generateInvoicePdf - Planned), FEAT-199 (createOutgoingTransaction - In Progress), FEAT-200 (listTransactions - In Progress), FEAT-207 (deleteOutgoingTransaction - In Progress), FEAT-210 (listPlatformCoaches - In Progress)
// User Types from Notion: CMS (admin/superadmin)
// Figma: https://www.figma.com/design/8KEwRuOoD5IgxTtFAtLlyS/Just_Do_Ad-1.2?node-id=8452-198211&t=n19jujPl9CMQvyEQ-4
// Comments from Notion: Let's do a TransactionGrid here, will be more resilient and better for UX than the card list

import { useLocale, useTranslations } from 'next-intl';
import { TLocale } from '@maany_shr/e-class-translations';

interface TransactionsProps {
  locale: TLocale;
  platformSlug: string;
}

export default function Transactions({ locale, platformSlug }: TransactionsProps) {
  const t = useTranslations('pages.cmsTransactions');
  const currentLocale = useLocale() as TLocale;

  return (
    <div className="flex flex-col space-y-5 px-30">
      <div>
        <h1>{t('title')}</h1>
        <p>{t('description')}</p>
      </div>

      {/* UI Components from Notion:
          - AG Grid
          - TransactionGrid (more resilient and better for UX than the card list)
      */}
    </div>
  );
}

'use client';

// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/CMS-Specific-Platform-Users-Manage-Users-d63184e64b3d41f5a780b08567518796
// Usecases from Notion: listUsers, sendNotification
// Features from Notion: FEAT-195 (listUsers - In Progress), FEAT-211 (sendNotification - In Progress)
// User Types from Notion: CMS (admin/superadmin - middleware enforced)
// Figma: https://www.figma.com/design/8KEwRuOoD5IgxTtFAtLlyS/Just_Do_Ad-1.2?node-id=8441-211960&t=VMezG7e44vUaPrMu-4
// Tags: AG Grid, users, notifications, bulk
// Comments from Notion: This is the only place where we'll be able to send bulk notifications, since here we have access to the platform and notifications need a platform

import { useLocale, useTranslations } from 'next-intl';
import { TLocale } from '@maany_shr/e-class-translations';

interface PlatformUsersProps {
  locale: TLocale;
  platformSlug: string;
}

export default function PlatformUsers({ locale, platformSlug }: PlatformUsersProps) {
  const t = useTranslations('pages.cmsPlatformUsers');
  const currentLocale = useLocale() as TLocale;

  return (
    <div className="flex flex-col space-y-5 px-30">
      <div>
        <h1>{t('title')}</h1>
        <p>{t('description')}</p>
      </div>

      {/* Features from Notion:
          - FEAT-195 (listUsers - In Progress)
          - FEAT-211 (sendNotification - In Progress)
      */}
      {/* UI Components from Notion:
          - UserGrid for user management
          - Bulk notification functionality
          - 3 components referenced
      */}
    </div>
  );
}

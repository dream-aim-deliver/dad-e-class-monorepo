// Auto-generated by page-scaffold command v2
// Notion: https://www.notion.so/CMS-Manage-Legal-Texts-5f461ddb9426476aa3816c88dea1ec9e
// Usecases: getPlatformLanguage, savePlatformLanguageLegalTexts
// Features: FEAT-87 (getPlatformLanguage), FEAT-215 (savePlatformLanguageLegalTexts)
// User Types: CMS

import { HydrateClient, prefetch, getServerTRPC } from '../config/trpc/cms-server';
import ManageLegalTexts from '../../client/pages/manage-legal-texts';
import { Suspense } from 'react';
import DefaultLoadingWrapper from '../../client/wrappers/default-loading';
import { TLocale } from '@maany_shr/e-class-translations';

interface ManageLegalTextsServerComponentProps {
  locale: TLocale;
  platformSlug: string;
  platformLocale: string;
  initialTab?: string;
}

export default async function ManageLegalTextsServerComponent(
  props: ManageLegalTextsServerComponentProps
) {
  const trpc = getServerTRPC({
    platform_slug: props.platformSlug,
    platform_locale: props.platformLocale,
  });

  // await Promise.all([
    prefetch(trpc.getPlatformLanguage.queryOptions({}))
  // ]);

  return (
    <HydrateClient>
      <Suspense fallback={<DefaultLoadingWrapper />}>
        <ManageLegalTexts initialTab={props.initialTab} />
      </Suspense>
    </HydrateClient>
  );
}
